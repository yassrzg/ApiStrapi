'use strict';

/**
 * tarif-prestation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tarif-prestation.tarif-prestation');
